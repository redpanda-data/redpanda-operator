---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: stretchclusters.cluster.redpanda.com
spec:
  group: cluster.redpanda.com
  names:
    kind: StretchCluster
    listKind: StretchClusterList
    plural: stretchclusters
    shortNames:
    - stretch
    singular: stretchcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=="LicenseValid")].message
      name: License
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: StretchCluster defines the CRD for StretchCluster configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              auditLogging:
                description: Defines audit logging settings.
                properties:
                  clientMaxBufferSize:
                    description: Defines the number of bytes (in bytes) allocated
                      by the internal audit client for audit messages.
                    type: integer
                  enabled:
                    description: Specifies whether to enable audit logging or not
                    type: boolean
                  enabledEventTypes:
                    description: Event types that should be captured by audit logs
                    items:
                      type: string
                    type: array
                  excludedPrincipals:
                    description: List of principals to exclude from auditing
                    items:
                      type: string
                    type: array
                  excludedTopics:
                    description: List of topics to exclude from auditing
                    items:
                      type: string
                    type: array
                  listener:
                    description: Kafka external listener name, note that it must have
                      `authenticationMethod` set to sasl
                    type: string
                  partitions:
                    description: Integer value defining the number of partitions used
                      by a newly created audit topic
                    type: integer
                  queueDrainIntervalMs:
                    description: In ms, frequency in which per shard audit logs are
                      batched to client for write to audit log.
                    type: integer
                  queueMaxBufferSizePerShard:
                    description: Defines the maximum amount of memory used (in bytes)
                      by the audit buffer in each shard
                    type: integer
                  replicationFactor:
                    description: |-
                      Defines the replication factor for a newly created audit log topic. This configuration applies
                      only to the audit log topic and may be different from the cluster or other topic configurations.
                      This cannot be altered for existing audit log topics. Setting this value is optional. If a value is not provided,
                      Redpanda will use the `internal_topic_replication_factor` cluster config value. Default is `null`
                    type: integer
                type: object
              auth:
                description: Defines authentication settings for listeners.
                properties:
                  sasl:
                    description: Configures SASL authentication in the Helm values.
                    properties:
                      bootstrapUser:
                        description: Specifies configuration about the bootstrap user.
                        properties:
                          mechanism:
                            description: Specifies the authentication mechanism to
                              use for the bootstrap user. Options are `SCRAM-SHA-256`
                              and `SCRAM-SHA-512`.
                            type: string
                          name:
                            description: |-
                              Name specifies the name of the bootstrap user created for the cluster, if unspecified
                              defaults to "kubernetes-controller".
                            type: string
                          secretKeyRef:
                            description: |-
                              Specifies the location where the generated password will be written or a pre-existing
                              password will be read from.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      enabled:
                        description: Enables SASL authentication. If you enable SASL
                          authentication, you must provide a Secret name in `secretRef`.
                        type: boolean
                      mechanism:
                        description: Specifies the default authentication mechanism
                          to use for superusers. Options are `SCRAM-SHA-256` and `SCRAM-SHA-512`.
                        type: string
                      secretRef:
                        description: If `users` is empty, `secretRef` specifies the
                          name of the Secret that contains your superuser credentials
                          in the format <username>:<password>:<optional-authentication-mechanism>.
                          Otherwise, `secretRef` specifies the name of the Secret
                          that the chart creates to store the credentials in `users`.
                        type: string
                      users:
                        description: Specifies a list of superuser credentials.
                        items:
                          description: UsersItems configures a list of superusers
                            in the Helm values.
                          properties:
                            mechanism:
                              description: Specifies the authentication mechanism
                                to use for superusers. Overrides the default in `SASL`.
                                Options are `SCRAM-SHA-256` and `SCRAM-SHA-512`.
                              type: string
                            name:
                              description: Specifies the name of the superuser.
                              type: string
                            password:
                              description: Specifies the superuser password.
                              type: string
                          type: object
                        type: array
                    type: object
                type: object
              clusterDomain:
                description: Customizes the Kubernetes cluster domain. This domain
                  is used to generate the internal domains of the StatefulSet Pods.
                  The default is the `cluster.local` domain.
                type: string
              commonLabels:
                additionalProperties:
                  type: string
                description: Assigns custom labels to all resources generated by the
                  operator. Specify labels as key/value pairs.
                type: object
              config:
                description: Defines configuration properties supported by Redpanda.
                properties:
                  cluster:
                    description: Specifies cluster configuration properties. See https://docs.redpanda.com/current/reference/cluster-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  extraClusterConfiguration:
                    additionalProperties:
                      description: |-
                        ClusterConfigValue represents a value of arbitrary type T. Values are string-encoded according to
                        YAML rules in order to preserve numerical fidelity.
                        Because these values must be embedded in a `.bootstrap.yaml` file - during the processing of
                        which, the AdminAPI's schema is unavailable - we endeavour to use yaml-compatible representations
                        throughout. The octet sequence of a representation will be inserted into a bootstrap template
                        verbatim.
                      properties:
                        configMapKeyRef:
                          description: |-
                            If the value is supplied by a kubernetes object reference, coordinates are embedded here.
                            For target values, the string value fetched from the source will be treated as
                            a raw string (and appropriately quoted for use in the bootstrap file) unless `useRawValue` is set.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        externalSecretRef:
                          description: |-
                            Deprecated: replaced by "externalSecretRefSelector"; this field should be *REMOVED* ASAP since it's barely
                            made it into service.
                          type: string
                        externalSecretRefSelector:
                          description: |-
                            If the value is supplied by an external source, coordinates are embedded here.
                            Note: we interpret all fetched external secrets as raw string values by default
                            and yam-encode them prior to embedding. To disable that behaviour, set `useRawValue`.
                          properties:
                            name:
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - name
                          type: object
                          x-kubernetes-map-type: atomic
                        repr:
                          description: |-
                            If the value is directly known, its YAML-compatible representation can be embedded here.
                            Use the string representation of a serialised value in order to preserve accuracy.
                            Prefer JSON-encoding for values that have multi-line representations in YAML.
                            Example:
                            The string "foo" should be the five octets "\"foo\""
                            A true value should be the four octets "true".
                            The number -123456 should be a seven-octet sequence, "-123456".
                          type: string
                        secretKeyRef:
                          description: |-
                            Should the value be contained in a k8s secret rather than configmap, we can refer
                            to it here.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        useRawValue:
                          description: |-
                            Any referenced value (from kubernetes or external lookup) is typically considered to be a raw string;
                            by default it'll be quoted as a string after its lookup is resolved. To skip that behaviour,
                            and to consider the external value verbatim (ie, if it's already in an appropriate serialized form
                            for use in the bootstrap configuration), set useRawValue to true.
                            In particular, this value should be set to `true` if your external source contains a numeric value.
                          type: boolean
                      type: object
                    description: |-
                      Holds values (or references to values) that should be used to configure the cluster; these
                      are resolved late in order to avoid embedding secrets directly into bootstrap configurations
                      exposed as Kubernetes configmaps.
                    type: object
                  node:
                    description: Specifies broker configuration properties. See https://docs.redpanda.com/current/reference/node-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  pandaproxy_client:
                    description: Specifies tunable configuration properties. See https://docs.redpanda.com/current/reference/tunable-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  rpk:
                    description: Specifies cluster configuration properties. See https://docs.redpanda.com/current/reference/cluster-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  schema_registry_client:
                    description: Specifies tunable configuration properties. See https://docs.redpanda.com/current/reference/tunable-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  tunable:
                    description: Specifies tunable configuration properties. See https://docs.redpanda.com/current/reference/tunable-properties/.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              enterprise:
                description: Defines an Enterprise license.
                properties:
                  license:
                    description: Specifies the Enterprise license key.
                    type: string
                  licenseSecretRef:
                    description: Defines a reference to a Secret resource that contains
                      the Enterprise license key.
                    properties:
                      key:
                        description: Specifies the key that is contains the Enterprise
                          license in the Secret.
                        type: string
                      name:
                        description: Specifies the name of the Secret resource to
                          use.
                        type: string
                    type: object
                type: object
              external:
                description: Defines external access settings.
                properties:
                  addresses:
                    description: Specifies addresses for the external listeners to
                      advertise.Provide one entry for each broker in order of StatefulSet
                      replicas. The number of brokers is defined in `statefulset.replicas`.
                      The values can be IP addresses or DNS names. If `external.domain`
                      is set, the domain is appended to these values.
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: Adds custom annotations to the external Service.
                    type: object
                  domain:
                    description: Specifies the domain to advertise to external clients.
                      If specified, then it will be appended to the `external.addresses`
                      values as each broker's advertised address.
                    type: string
                  enabled:
                    description: Specifies whether the external access is enabled.
                    type: boolean
                  externalDns:
                    description: Defines externalDNS configurations.
                    properties:
                      enabled:
                        description: Specifies whether externalDNS annotations are
                          added to LoadBalancer Services. If you enable externalDns,
                          each LoadBalancer Service defined in `external.type` will
                          be annotated with an external-dns hostname that matches
                          `external.addresses[i]`.`external.domain`.
                        type: boolean
                    type: object
                  prefixTemplate:
                    description: Specifies a naming prefix template for external Services.
                    type: string
                  service:
                    description: Configures the external Service resource.
                    properties:
                      enabled:
                        description: Specifies whether to create the external Service.
                          If set to `false`, the external Service type is not created.
                          You can still set your cluster with external access but
                          not create the supporting Service. Set this to `false` to
                          manage your own Service.
                        type: boolean
                    type: object
                  sourceRanges:
                    description: Source range for external access. Only applicable
                      when `external.type` is LoadBalancer.
                    items:
                      type: string
                    type: array
                  type:
                    description: Specifies the external Service type. Only NodePort
                      and LoadBalancer are supported. If undefined, then advertised
                      listeners will be configured in Redpanda, but the Helm chart
                      will not create a Service. NodePort is recommended in cases
                      where latency is a priority.
                    type: string
                type: object
              image:
                description: Defines the container image settings to use for the Redpanda
                  cluster.
                properties:
                  pullPolicy:
                    description: Specifies the strategy used for pulling images from
                      the repository. For available values, see https://kubernetes.io/docs/concepts/containers/images/#image-pull-policy.
                    type: string
                  repository:
                    description: Specifies the image repository to pull from.
                    type: string
                  tag:
                    description: Specifies the image tag.
                    type: string
                type: object
              imagePullSecrets:
                description: Specifies credentials for a private image repository.
                  For details, see https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              listeners:
                description: Defines settings for listeners, including HTTP Proxy,
                  Schema Registry, the Admin API and the Kafka API.
                properties:
                  admin:
                    description: Configures settings for the Admin API listeners.
                    properties:
                      appProtocol:
                        type: string
                      authenticationMethod:
                        description: Specifies the authentication method for this
                          listener.
                        type: string
                      enabled:
                        description: Specifies whether this Listener is enabled.
                        type: boolean
                      external:
                        additionalProperties:
                          description: |-
                            StretchExternalListener configures an external listener in stretch clusters.

                            Forked from ExternalListener: embeds StretchListener instead of Listener.
                          properties:
                            advertisedPorts:
                              description: Specifies the network port that the external
                                Service listens on.
                              items:
                                format: int32
                                type: integer
                              type: array
                            appProtocol:
                              type: string
                            authenticationMethod:
                              description: Specifies the authentication method for
                                this listener.
                              type: string
                            enabled:
                              description: Specifies whether this Listener is enabled.
                              type: boolean
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              description: Specifies the container port number for
                                this listener.
                              format: int32
                              type: integer
                            prefixTemplate:
                              description: Specifies the template used for generating
                                the advertised addresses of Services.
                              type: string
                            tls:
                              description: Configures TLS settings for the internal
                                listener.
                              properties:
                                cert:
                                  description: References a specific certificate for
                                    the listener.
                                  type: string
                                enabled:
                                  description: Specifies whether TLS is enabled for
                                    the listener.
                                  type: boolean
                                requireClientAuth:
                                  description: Indicates whether client authentication
                                    (mTLS) is required.
                                  type: boolean
                                trustStore:
                                  description: TrustStore allows setting the `truststore_path`
                                    on this listener.
                                  maxProperties: 1
                                  minProperties: 1
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key
                                        of a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              type: object
                          type: object
                        description: Defines settings for the external listeners.
                        type: object
                      port:
                        description: Specifies the container port number for this
                          listener.
                        format: int32
                        type: integer
                      prefixTemplate:
                        description: Specifies the template used for generating the
                          advertised addresses of Services.
                        type: string
                      tls:
                        description: Configures TLS settings for the internal listener.
                        properties:
                          cert:
                            description: References a specific certificate for the
                              listener.
                            type: string
                          enabled:
                            description: Specifies whether TLS is enabled for the
                              listener.
                            type: boolean
                          requireClientAuth:
                            description: Indicates whether client authentication (mTLS)
                              is required.
                            type: boolean
                          trustStore:
                            description: TrustStore allows setting the `truststore_path`
                              on this listener.
                            maxProperties: 1
                            minProperties: 1
                            properties:
                              configMapKeyRef:
                                description: Selects a key from a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        type: object
                    type: object
                  http:
                    description: Configures settings for the HTTP Proxy listeners.
                    properties:
                      appProtocol:
                        type: string
                      authenticationMethod:
                        description: Specifies the authentication method for this
                          listener.
                        type: string
                      enabled:
                        description: Specifies whether this Listener is enabled.
                        type: boolean
                      external:
                        additionalProperties:
                          description: |-
                            StretchExternalListener configures an external listener in stretch clusters.

                            Forked from ExternalListener: embeds StretchListener instead of Listener.
                          properties:
                            advertisedPorts:
                              description: Specifies the network port that the external
                                Service listens on.
                              items:
                                format: int32
                                type: integer
                              type: array
                            appProtocol:
                              type: string
                            authenticationMethod:
                              description: Specifies the authentication method for
                                this listener.
                              type: string
                            enabled:
                              description: Specifies whether this Listener is enabled.
                              type: boolean
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              description: Specifies the container port number for
                                this listener.
                              format: int32
                              type: integer
                            prefixTemplate:
                              description: Specifies the template used for generating
                                the advertised addresses of Services.
                              type: string
                            tls:
                              description: Configures TLS settings for the internal
                                listener.
                              properties:
                                cert:
                                  description: References a specific certificate for
                                    the listener.
                                  type: string
                                enabled:
                                  description: Specifies whether TLS is enabled for
                                    the listener.
                                  type: boolean
                                requireClientAuth:
                                  description: Indicates whether client authentication
                                    (mTLS) is required.
                                  type: boolean
                                trustStore:
                                  description: TrustStore allows setting the `truststore_path`
                                    on this listener.
                                  maxProperties: 1
                                  minProperties: 1
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key
                                        of a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              type: object
                          type: object
                        description: Defines settings for the external listeners.
                        type: object
                      port:
                        description: Specifies the container port number for this
                          listener.
                        format: int32
                        type: integer
                      prefixTemplate:
                        description: Specifies the template used for generating the
                          advertised addresses of Services.
                        type: string
                      tls:
                        description: Configures TLS settings for the internal listener.
                        properties:
                          cert:
                            description: References a specific certificate for the
                              listener.
                            type: string
                          enabled:
                            description: Specifies whether TLS is enabled for the
                              listener.
                            type: boolean
                          requireClientAuth:
                            description: Indicates whether client authentication (mTLS)
                              is required.
                            type: boolean
                          trustStore:
                            description: TrustStore allows setting the `truststore_path`
                              on this listener.
                            maxProperties: 1
                            minProperties: 1
                            properties:
                              configMapKeyRef:
                                description: Selects a key from a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        type: object
                    type: object
                  kafka:
                    description: Configures settings for the Kafka API listeners.
                    properties:
                      appProtocol:
                        type: string
                      authenticationMethod:
                        description: Specifies the authentication method for this
                          listener.
                        type: string
                      enabled:
                        description: Specifies whether this Listener is enabled.
                        type: boolean
                      external:
                        additionalProperties:
                          description: |-
                            StretchExternalListener configures an external listener in stretch clusters.

                            Forked from ExternalListener: embeds StretchListener instead of Listener.
                          properties:
                            advertisedPorts:
                              description: Specifies the network port that the external
                                Service listens on.
                              items:
                                format: int32
                                type: integer
                              type: array
                            appProtocol:
                              type: string
                            authenticationMethod:
                              description: Specifies the authentication method for
                                this listener.
                              type: string
                            enabled:
                              description: Specifies whether this Listener is enabled.
                              type: boolean
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              description: Specifies the container port number for
                                this listener.
                              format: int32
                              type: integer
                            prefixTemplate:
                              description: Specifies the template used for generating
                                the advertised addresses of Services.
                              type: string
                            tls:
                              description: Configures TLS settings for the internal
                                listener.
                              properties:
                                cert:
                                  description: References a specific certificate for
                                    the listener.
                                  type: string
                                enabled:
                                  description: Specifies whether TLS is enabled for
                                    the listener.
                                  type: boolean
                                requireClientAuth:
                                  description: Indicates whether client authentication
                                    (mTLS) is required.
                                  type: boolean
                                trustStore:
                                  description: TrustStore allows setting the `truststore_path`
                                    on this listener.
                                  maxProperties: 1
                                  minProperties: 1
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key
                                        of a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              type: object
                          type: object
                        description: Defines settings for the external listeners.
                        type: object
                      port:
                        description: Specifies the container port number for this
                          listener.
                        format: int32
                        type: integer
                      prefixTemplate:
                        description: Specifies the template used for generating the
                          advertised addresses of Services.
                        type: string
                      tls:
                        description: Configures TLS settings for the internal listener.
                        properties:
                          cert:
                            description: References a specific certificate for the
                              listener.
                            type: string
                          enabled:
                            description: Specifies whether TLS is enabled for the
                              listener.
                            type: boolean
                          requireClientAuth:
                            description: Indicates whether client authentication (mTLS)
                              is required.
                            type: boolean
                          trustStore:
                            description: TrustStore allows setting the `truststore_path`
                              on this listener.
                            maxProperties: 1
                            minProperties: 1
                            properties:
                              configMapKeyRef:
                                description: Selects a key from a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        type: object
                    type: object
                  rpc:
                    description: Configures settings for the RPC API listener.
                    properties:
                      port:
                        description: Specifies the container port number for the internal
                          listener.
                        type: integer
                      tls:
                        description: Configures TLS settings for the internal listener.
                        properties:
                          cert:
                            description: References a specific certificate for the
                              listener.
                            type: string
                          enabled:
                            description: Specifies whether TLS is enabled for the
                              listener.
                            type: boolean
                          requireClientAuth:
                            description: Indicates whether client authentication (mTLS)
                              is required.
                            type: boolean
                          trustStore:
                            description: TrustStore allows setting the `truststore_path`
                              on this listener.
                            maxProperties: 1
                            minProperties: 1
                            properties:
                              configMapKeyRef:
                                description: Selects a key from a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        type: object
                    type: object
                  schemaRegistry:
                    description: Configures settings for the Schema Registry listeners.
                    properties:
                      appProtocol:
                        type: string
                      authenticationMethod:
                        description: Specifies the authentication method for this
                          listener.
                        type: string
                      enabled:
                        description: Specifies whether this Listener is enabled.
                        type: boolean
                      external:
                        additionalProperties:
                          description: |-
                            StretchExternalListener configures an external listener in stretch clusters.

                            Forked from ExternalListener: embeds StretchListener instead of Listener.
                          properties:
                            advertisedPorts:
                              description: Specifies the network port that the external
                                Service listens on.
                              items:
                                format: int32
                                type: integer
                              type: array
                            appProtocol:
                              type: string
                            authenticationMethod:
                              description: Specifies the authentication method for
                                this listener.
                              type: string
                            enabled:
                              description: Specifies whether this Listener is enabled.
                              type: boolean
                            nodePort:
                              format: int32
                              type: integer
                            port:
                              description: Specifies the container port number for
                                this listener.
                              format: int32
                              type: integer
                            prefixTemplate:
                              description: Specifies the template used for generating
                                the advertised addresses of Services.
                              type: string
                            tls:
                              description: Configures TLS settings for the internal
                                listener.
                              properties:
                                cert:
                                  description: References a specific certificate for
                                    the listener.
                                  type: string
                                enabled:
                                  description: Specifies whether TLS is enabled for
                                    the listener.
                                  type: boolean
                                requireClientAuth:
                                  description: Indicates whether client authentication
                                    (mTLS) is required.
                                  type: boolean
                                trustStore:
                                  description: TrustStore allows setting the `truststore_path`
                                    on this listener.
                                  maxProperties: 1
                                  minProperties: 1
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key from a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: SecretKeySelector selects a key
                                        of a Secret.
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              type: object
                          type: object
                        description: Defines settings for the external listeners.
                        type: object
                      port:
                        description: Specifies the container port number for this
                          listener.
                        format: int32
                        type: integer
                      prefixTemplate:
                        description: Specifies the template used for generating the
                          advertised addresses of Services.
                        type: string
                      tls:
                        description: Configures TLS settings for the internal listener.
                        properties:
                          cert:
                            description: References a specific certificate for the
                              listener.
                            type: string
                          enabled:
                            description: Specifies whether TLS is enabled for the
                              listener.
                            type: boolean
                          requireClientAuth:
                            description: Indicates whether client authentication (mTLS)
                              is required.
                            type: boolean
                          trustStore:
                            description: TrustStore allows setting the `truststore_path`
                              on this listener.
                            maxProperties: 1
                            minProperties: 1
                            properties:
                              configMapKeyRef:
                                description: Selects a key from a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: SecretKeySelector selects a key of a
                                  Secret.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        type: object
                    type: object
                type: object
              logging:
                description: Defines the log level settings.
                properties:
                  logLevel:
                    description: Specifies the log level.
                    type: string
                  usageStats:
                    description: Defines settings for usage statistics reporting.
                    properties:
                      enabled:
                        description: Specifies whether usage statistics are enabled.
                        type: boolean
                    type: object
                type: object
              monitoring:
                description: Defines settings for monitoring Redpanda.
                properties:
                  enableHttp2:
                    type: boolean
                  enabled:
                    description: Specifies whether to create a ServiceMonitor that
                      can be used by Prometheus Operator or VictoriaMetrics Operator
                      to scrape the metrics.
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    description: Adds custom labels to the ServiceMonitor resource.
                    type: object
                  scrapeInterval:
                    description: Specifies how often to scrape metrics.
                    type: string
                  tlsConfig:
                    description: Specifies tls configuration properties.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              rackAwareness:
                description: Defines rack awareness settings.
                properties:
                  enabled:
                    description: Specifies whether rack awareness is enabled. When
                      enabled, Kubernetes failure zones are treated as racks. Redpanda
                      maps each rack to a failure zone and places partition replicas
                      across them. Requires `rbac.enabled` set to `true`.
                    type: boolean
                  nodeAnnotation:
                    description: Specifies the key in Node labels or annotations to
                      use to denote failure zones.
                    type: string
                type: object
              rbac:
                description: Defines Role Based Access Control (RBAC) settings.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Adds custom annotations to the RBAC resources.
                    type: object
                  enabled:
                    description: Whether RBAC is enabled. Enable for features that
                      need extra privileges, such as rack awareness. If you use the
                      Redpanda Operator, you must deploy it with the `--set rbac.createRPKBundleCRs=true`
                      flag to give it the required ClusterRoles.
                    type: boolean
                  rpkDebugBundle:
                    type: boolean
                type: object
              resources:
                description: Defines container resource settings.
                properties:
                  cpu:
                    description: Specifies the number of CPU cores.
                    properties:
                      cores:
                        anyOf:
                        - type: integer
                        - type: string
                        description: 'Specifies the number of CPU cores available
                          to the application. Redpanda makes use of a thread per core
                          model. For details, see https://docs.redpanda.com/current/get-started/architecture/#thread-per-core-model.
                          For this reason, Redpanda should only be given full cores.
                          Note: You can increase cores, but decreasing cores is not
                          currently supported. See the GitHub issue:https://github.com/redpanda-data/redpanda/issues/350.
                          This setting is equivalent to `--smp`, `resources.requests.cpu`,
                          and `resources.limits.cpu`. For production, use `4` or greater.'
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      overprovisioned:
                        description: 'Specifies whether Redpanda assumes it has all
                          of the provisioned CPU. This should be `true` unless the
                          container has CPU affinity. Equivalent to: `--idle-poll-time-us
                          0`, `--thread-affinity 0`, and `--poll-aio 0`. If the value
                          of full cores in `resources.cpu.cores` is less than `1`,
                          this setting is set to `true`.'
                        type: boolean
                    type: object
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Standard K8s resource limits.
                    type: object
                  memory:
                    description: Specifies the amount of memory.
                    properties:
                      container:
                        description: Defines resource limits for containers.
                        properties:
                          max:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Specifies the maximum resources that can
                              be allocated to a container.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          min:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Specifies the minimum resources required
                              for a container.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                      enable_memory_locking:
                        description: Enables memory locking. For production, set to
                          `true`.
                        type: boolean
                      redpanda:
                        description: Allows you to optionally specify the memory size
                          for both the Redpanda process and the underlying reserved
                          memory used by Seastar.
                        properties:
                          memory:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Memory for the Redpanda process. This must
                              be lower than the container's memory (`resources.memory.container.min`
                              if provided, otherwise `resources.memory.container.max`).
                              Equivalent to `--memory`. For production, use 8Gi or
                              greater.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          reserveMemory:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Memory reserved for the OS. Any value above
                              1Gi will provide diminishing performance benefits. Equivalent
                              to `--reserve-memory`. For production, use 1Gi.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Standard K8s resource requests.
                    type: object
                type: object
              service:
                description: Defines settings for the headless ClusterIP Service.
                properties:
                  internal:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  name:
                    type: string
                type: object
              serviceAccount:
                description: Defines Service account settings.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Adds custom annotations to the ServiceAccount resources.
                    type: object
                  automountServiceAccountToken:
                    description: Specifies whether a service account should automount
                      API-Credentials
                    type: boolean
                  create:
                    description: Specifies whether a ServiceAccount should be created.
                    type: boolean
                  name:
                    description: Specifies the name of the ServiceAccount.
                    type: string
                type: object
              storage:
                description: Defines storage settings for the Redpanda data directory
                  and the Tiered Storage cache.
                properties:
                  hostPath:
                    description: Specifies the absolute path on the worker node to
                      store the Redpanda data directory.
                    type: string
                  persistentVolume:
                    description: Configures a PersistentVolumeClaim (PVC) template
                      to create for each Pod.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Adds annotations to the PersistentVolumeClaims
                          to provide additional information or metadata that can be
                          used by other tools or libraries.
                        type: object
                      enabled:
                        description: Specifies whether to enable the Helm chart to
                          create PersistentVolumeClaims for Pods.
                        type: boolean
                      labels:
                        additionalProperties:
                          type: string
                        description: Applies labels to the PersistentVolumeClaims
                          to facilitate identification and selection based on custom
                          criteria.
                        type: object
                      nameOverwrite:
                        description: Option to change volume claim template name for
                          tiered storage persistent volume if tiered.mountType is
                          set to `persistentVolume`
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Specifies the storage capacity required.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClass:
                        description: Specifies the StorageClass for the PersistentVolumeClaims
                          to determine how PersistentVolumes are provisioned and managed.
                        type: string
                    type: object
                  tiered:
                    description: Configures storage for the Tiered Storage cache.
                    properties:
                      config:
                        description: Configures Tiered Storage properties.
                        properties:
                          cloud_storage_api_endpoint:
                            type: string
                          cloud_storage_api_endpoint_port:
                            type: integer
                          cloud_storage_azure_adls_endpoint:
                            type: string
                          cloud_storage_azure_adls_port:
                            type: integer
                          cloud_storage_azure_container:
                            type: string
                          cloud_storage_azure_managed_identity_id:
                            type: string
                          cloud_storage_azure_shared_key:
                            type: string
                          cloud_storage_azure_storage_account:
                            type: string
                          cloud_storage_bucket:
                            type: string
                          cloud_storage_cache_check_interval:
                            type: integer
                          cloud_storage_cache_directory:
                            type: string
                          cloud_storage_cache_size:
                            type: string
                          cloud_storage_credentials_source:
                            type: string
                          cloud_storage_disable_tls:
                            type: boolean
                          cloud_storage_enable_remote_read:
                            type: boolean
                          cloud_storage_enable_remote_write:
                            type: boolean
                          cloud_storage_enabled:
                            type: boolean
                          cloud_storage_initial_backoff_ms:
                            type: integer
                          cloud_storage_manifest_upload_timeout_ms:
                            type: integer
                          cloud_storage_max_connection_idle_time_ms:
                            type: integer
                          cloud_storage_max_connections:
                            type: integer
                          cloud_storage_region:
                            type: string
                          cloud_storage_segment_max_upload_interval_sec:
                            type: integer
                          cloud_storage_segment_upload_timeout_ms:
                            type: integer
                          cloud_storage_trust_file:
                            type: string
                          cloud_storage_upload_ctrl_d_coeff:
                            type: integer
                          cloud_storage_upload_ctrl_max_shares:
                            type: integer
                          cloud_storage_upload_ctrl_min_shares:
                            type: integer
                          cloud_storage_upload_ctrl_p_coeff:
                            type: integer
                          cloud_storage_upload_ctrl_update_interval_ms:
                            type: integer
                        type: object
                      credentialsSecretRef:
                        description: CredentialsSecretRef sets cloud_storage_secret_key
                          and/or cloud_storage_access_key from a referenced Secret.
                        properties:
                          accessKey:
                            properties:
                              configurationKey:
                                type: string
                              key:
                                type: string
                              name:
                                type: string
                            type: object
                          secretKey:
                            properties:
                              configurationKey:
                                type: string
                              key:
                                type: string
                              name:
                                type: string
                            type: object
                        type: object
                      hostPath:
                        description: Specifies the absolute path on the worker node
                          to store the Tiered Storage cache.
                        type: string
                      mountType:
                        description: 'mountType: "none", "hostPath", "emptyDir", or
                          "persistentVolume".'
                        type: string
                      persistentVolume:
                        description: Configures a PersistentVolumeClaim (PVC) template
                          for the Tiered Storage cache.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Adds annotations to the PersistentVolumeClaims
                              to provide additional information or metadata that can
                              be used by other tools or libraries.
                            type: object
                          enabled:
                            description: Specifies whether to enable the Helm chart
                              to create PersistentVolumeClaims for Pods.
                            type: boolean
                          labels:
                            additionalProperties:
                              type: string
                            description: Applies labels to the PersistentVolumeClaims
                              to facilitate identification and selection based on
                              custom criteria.
                            type: object
                          nameOverwrite:
                            description: Option to change volume claim template name
                              for tiered storage persistent volume if tiered.mountType
                              is set to `persistentVolume`
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Specifies the storage capacity required.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClass:
                            description: Specifies the StorageClass for the PersistentVolumeClaims
                              to determine how PersistentVolumes are provisioned and
                              managed.
                            type: string
                        type: object
                    type: object
                type: object
              tls:
                description: Defines TLS settings for listeners.
                properties:
                  certs:
                    additionalProperties:
                      description: Certificate configures TLS certificates.
                      properties:
                        applyInternalDNSNames:
                          description: Specifies you wish to have Kubernetes internal
                            dns names (IE the headless service of the redpanda StatefulSet)
                            included in `dnsNames` of the  certificate even, when
                            supplying an issuer.
                          type: boolean
                        caEnabled:
                          description: Specifies whether to include the `ca.crt` file
                            in the trust stores of all listeners. Set to `true` only
                            for certificates that are not authenticated using public
                            certificate authorities (CAs).
                          type: boolean
                        clientSecretRef:
                          description: Specify the name of an existing Secret resource
                            that contains your client TLS certificate.
                          properties:
                            name:
                              description: Specifies the name of the Secret resource.
                              type: string
                          type: object
                        duration:
                          description: Specifies the validity duration of certificates
                            generated with `issuerRef`.
                          type: string
                        enabled:
                          type: boolean
                        issuerRef:
                          description: Specify the name of an existing Issuer or ClusterIssuer
                            resource to use to generate certificates. Requires cert-manager.
                            See https://cert-manager.io/v1.1-docs.
                          properties:
                            group:
                              type: string
                            kind:
                              description: Specifies the kind of resource. One of
                                `Issuer` or `ClusterIssuer`.
                              type: string
                            name:
                              description: Specifies the name of the resource.
                              type: string
                          type: object
                        secretRef:
                          description: Specify the name of an existing Secret resource
                            that contains your TLS certificate.
                          properties:
                            name:
                              description: Specifies the name of the Secret resource.
                              type: string
                          type: object
                      type: object
                    description: Lists all available certificates in the cluster.
                      You can reference a specific certificate’s name in each listener’s
                      `listeners.<listener name>.tls.cert` setting.
                    type: object
                  enabled:
                    description: Enables TLS globally for all listeners. Each listener
                      must include a certificate name in its `<listener>.tls` object.
                      To allow you to enable TLS for individual listeners, certificates
                      are always loaded, even if TLS is disabled.
                    type: boolean
                type: object
              tuning:
                description: Defines settings for the autotuner tool in Redpanda.
                properties:
                  ballast_file_path:
                    description: Specifies the file path for ballast file.
                    type: string
                  ballast_file_size:
                    description: Defines the size of the ballast file.
                    type: string
                  tune_aio_events:
                    description: Specifies whether to increase the number of allowed
                      asynchronous IO events.
                    type: boolean
                  tune_ballast_file:
                    description: Specifies whether to create the ballast file.
                    type: boolean
                  tune_clocksource:
                    description: Specifies whether to synchronize NTP.
                    type: boolean
                  well_known_io:
                    description: Specifies the vendor, VM type, and storage device
                      type that Redpanda runs on, in the format <vendor>:<vm>:<storage>.
                    type: string
                type: object
            type: object
          status:
            default:
              conditions:
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: Ready
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: Healthy
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: LicenseValid
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: ResourcesSynced
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: ConfigurationApplied
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: Quiesced
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: NotReconciled
                status: Unknown
                type: Stable
            properties:
              conditions:
                description: Conditions holds the conditions for the StretchCluster.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configVersion:
                description: |-
                  ConfigVersion contains the configuration version written in
                  Redpanda used for restarting broker nodes as necessary.
                type: string
              nodePools:
                description: |-
                  NodePools contains information about the node pools associated
                  with this cluster.
                items:
                  description: EmbeddedNodePoolStatus defines the observed state of
                    any node pools tied to this cluster
                  properties:
                    condemnedReplicas:
                      description: |-
                        CondemnedReplicas is the number of replicas that will be decommissioned
                        as part of a scaling down operation.
                      format: int32
                      type: integer
                    desiredReplicas:
                      description: |-
                        DesiredReplicas is the number of replicas that ought to be
                        run for the cluster. It combines the desired replicas across
                        all node pools.
                      format: int32
                      type: integer
                    name:
                      description: Name is the name of the pool
                      type: string
                    outOfDateReplicas:
                      description: |-
                        OutOfDateReplicas is the number of replicas that don't currently
                        match their node pool definitions. If OutOfDateReplicas is not 0
                        it should mean that the operator will soon roll this many pods.
                      format: int32
                      type: integer
                    readyReplicas:
                      description: |-
                        ReadyReplicas is the number of replicas whose readiness probes are
                        currently passing.
                      format: int32
                      type: integer
                    replicas:
                      description: |-
                        Replicas is the number of actual replicas currently across
                        the node pool. This differs from DesiredReplicas during
                        a scaling operation, but should be the same once the cluster
                        has quiesced.
                      format: int32
                      type: integer
                    runningReplicas:
                      description: |-
                        RunningReplicas is the number of replicas that are actively in a running
                        state.
                      format: int32
                      type: integer
                    upToDateReplicas:
                      description: |-
                        UpToDateReplicas is the number of replicas that currently match
                        their node pool definitions.
                      format: int32
                      type: integer
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
