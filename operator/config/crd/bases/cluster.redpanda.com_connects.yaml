---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: connects.cluster.redpanda.com
spec:
  group: cluster.redpanda.com
  names:
    kind: Connect
    listKind: ConnectList
    plural: connects
    shortNames:
    - rpcn
    singular: connect
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Available
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Connect defines a Redpanda Connect pipeline managed by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
            type: string
          metadata:
            type: object
          spec:
            description: Defines the desired state of the Connect pipeline.
            properties:
              cluster:
                description: ClusterSource is a reference to the Redpanda cluster this pipeline connects to.
                properties:
                  clusterRef:
                    description: ClusterRef is a reference to the cluster where the object should be created.
                    properties:
                      group:
                        type: string
                      kind:
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  staticConfiguration:
                    description: StaticConfiguration holds connection parameters to Kafka and Admin APIs.
                    properties:
                      admin:
                        description: AdminAPISpec is the configuration information for communicating with the Admin API.
                        properties:
                          sasl:
                            properties:
                              authToken:
                                description: Specifies token for token-based authentication.
                                x-kubernetes-preserve-unknown-fields: true
                              mechanism:
                                type: string
                              password:
                                x-kubernetes-preserve-unknown-fields: true
                              username:
                                type: string
                            required:
                            - mechanism
                            type: object
                          tls:
                            x-kubernetes-preserve-unknown-fields: true
                          urls:
                            items:
                              type: string
                            type: array
                        required:
                        - urls
                        type: object
                      kafka:
                        description: Kafka is the configuration information for communicating with the Kafka API.
                        properties:
                          brokers:
                            items:
                              type: string
                            minItems: 1
                            type: array
                          sasl:
                            x-kubernetes-preserve-unknown-fields: true
                          tls:
                            x-kubernetes-preserve-unknown-fields: true
                        required:
                        - brokers
                        type: object
                      schemaRegistry:
                        description: SchemaRegistry is the configuration information for communicating with the Schema Registry API.
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                type: object
                x-kubernetes-validations:
                - message: either clusterRef or staticConfiguration must be set
                  rule: has(self.clusterRef) || has(self.staticConfiguration)
              configYaml:
                description: ConfigYAML is the Redpanda Connect pipeline configuration in YAML format.
                type: string
              env:
                description: Env specifies additional environment variables for the pipeline container.
                items:
                  description: EnvVar represents an environment variable present in a Container.
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value.
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  type: object
                type: array
              image:
                description: Image is the container image for the Redpanda Connect deployment.
                type: string
              licenseSecretRef:
                description: |-
                  LicenseSecretRef is a reference to a Secret containing the Redpanda enterprise license.
                  The license is required for Redpanda Connect to operate.
                properties:
                  key:
                    description: The key of the secret to select from. Must be a valid secret key.
                    type: string
                  name:
                    default: ""
                    description: Name of the referent.
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined.
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              paused:
                description: Paused stops the pipeline by scaling replicas to zero when set to true.
                type: boolean
              replicas:
                default: 1
                description: Replicas is the number of pipeline replicas to run.
                format: int32
                minimum: 0
                type: integer
              resources:
                description: Resources defines the compute resource requirements for the pipeline pods.
                properties:
                  claims:
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          type: string
                        request:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Limits describes the maximum amount of compute resources allowed.
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Requests describes the minimum amount of compute resources required.
                    type: object
                type: object
            required:
            - configYaml
            - licenseSecretRef
            type: object
          status:
            description: Represents the current observed state of the Connect pipeline.
            properties:
              conditions:
                description: Conditions holds the conditions for the Connect resource.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last observed generation of the Connect resource.
                format: int64
                type: integer
              phase:
                description: Phase describes the current phase of the pipeline lifecycle.
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready pipeline pods.
                format: int32
                type: integer
              replicas:
                description: Replicas is the number of desired replicas.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
