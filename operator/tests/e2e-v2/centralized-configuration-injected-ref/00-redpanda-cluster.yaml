apiVersion: v1
kind: ConfigMap
metadata:
  name: injected-value
  labels:
    app: redpanda-injected-ref
data:
  value-1: |
    1003
---
apiVersion: cluster.redpanda.com/v1alpha2
kind: Redpanda
metadata:
  name: redpanda-injected-ref
spec:
  clusterSpec:
    statefulset:
      replicas: 1
    config:
      extraClusterConfiguration:
        segment_appender_flush_timeout_ms:
          configMapKeyRef:
            name: injected-value
            key: value-1
          useRawValue: true