apiVersion: cluster.redpanda.com/v1alpha2
kind: Role
metadata:
  name: combined-role
spec:
  cluster:
    clusterRef:
      name: redpanda-cluster
  principals:
  - "User:charlie"
  - "User:dave"
  - "User:eve"
  # Remove authorization section to transition to principals-only mode