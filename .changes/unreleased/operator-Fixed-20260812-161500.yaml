project: operator
kind: Fixed
body: 'V1 rolling restarts no longer delete a pod when Redpanda refuses to enable maintenance mode: an inverted error check ignored every non-404 refusal (including the one-node-in-maintenance interlock, refused while the previously evicted broker''s flag is still held) and deleted the pod undrained, allowing two brokers down at once; genuine 404s (decommissioned broker) now proceed instead of requeueing forever'
time: 2026-08-12T16:15:00.000000000+02:00
